# Environment Setup

This project skeleton was created from `AteraSnipeSync_ENVIRONMENT_SETUP.md`.

Required tools:

- .NET 10 SDK
- Git
- Visual Studio 2022, VS Code, or Rider

Useful commands:

```powershell
dotnet --info
dotnet new list
dotnet restore
dotnet build
dotnet test
```
